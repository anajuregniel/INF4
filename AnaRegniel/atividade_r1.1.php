<?php
    // Declarar duas variáveis númericas
    $x = 2;
    $y = 1;

    // Realizar operações com as variáveis
    $soma = $x + $y;
    $sub = $x - $y;
    $prod = $x * $y;
    $div = $x / $y;

    // Exibir resultados
    echo "<p>X = " . $x;
    echo "<p>Y = " . $y;
    echo "<p>Soma = " . $soma;
    echo "<p>Diferença = " . $sub;
    echo "<p>Multiplicação = " . $prod;
    echo "<p>Divisão = " . $div;